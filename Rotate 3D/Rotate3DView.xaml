<Window x:Class="DnBim_Tool.Rotate_3D.Rotate3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DnBim_Tool.Rotate_3D"
             mc:Ignorable="d" 
             Height="165"
             Width="202" Title="Rotate 3D"
             ShowInTaskbar="True"
             ResizeMode="NoResize"
             WindowStartupLocation="CenterScreen"
          KeyDown="Window_KeyDown"
        >
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment ="Top" Margin="0 10 0 0">

            <GroupBox Header="Rotate around element" Width="180" Height="70" BorderBrush="Gray">
                <StackPanel Margin="0 5 0 0">

                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">

                        <Label Content="Rotata angle:" Width="100"/>
                        <TextBox x:Name="tbAngle" Width="60" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                    </StackPanel>

                </StackPanel>
            </GroupBox>

            <!--<GroupBox Header="Image" BorderBrush="Gray" Width="116" Height="160">
                <Image x:Name="image" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform"/>
            </GroupBox>-->

        </StackPanel>

        <StackPanel Orientation="Horizontal" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 5 5">
            <Button x:Name="btOk" Width="80" Content="OK" Margin="0 0 5 0" Click="btOk_Click" />
            <Button x:Name="btCancel" Width="80" Content="Cancel" Click="btCancel_Click"/>
        </StackPanel>

    </Grid>
</Window>
